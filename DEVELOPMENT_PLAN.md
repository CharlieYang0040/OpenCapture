# OpenCapture 개발 계획서

## 1. 프로젝트 목표

OpenCapture는 Windows 10/11에서 동작하는 고성능 오픈소스 화면 녹화, 화면 캡처, GIF 녹화 및 미디어 포맷 변환 애플리케이션이다.

캡처 대상은 게임 여부에 따라 구분하지 않고 다음 세 종류로 통합한다.

1. Windows 창 선택
2. 사용자 지정 화면 영역
3. 모니터 전체

선택한 대상은 동영상 녹화, GIF 녹화 및 정지 화면 캡처에 공통으로 사용한다. 게임 프로세스의 메모리를 읽거나 DLL을 주입하고 렌더링 함수를 후킹하는 방식은 사용하지 않는다.

## 2. 핵심 요구사항

- Windows 전용 네이티브 애플리케이션으로 개발한다.
- 핵심 엔진과 UI는 C++20으로 개발한다.
- UI는 간결하게 유지하고 녹화 성능을 최우선으로 한다.
- 화면 프레임은 가능한 한 GPU 메모리 안에서 처리한다.
- NVIDIA GPU에서는 FFmpeg NVENC를 기본 인코더로 사용한다.
- NVIDIA가 아닌 환경에서는 Intel QSV, AMD AMF, Media Foundation, 소프트웨어 H.264 순서로 폴백한다.
- 창, 영역, 모니터에 대해 녹화와 스크린샷을 모두 지원한다.
- 자주 사용하는 화면 영역을 이름이 있는 프리셋으로 저장하고 스크린샷과 GIF에 즉시 재사용한다.
- 스크린샷은 파일 저장 없이 클립보드로만 복사하는 경로를 제공한다.
- 게임 플레이 및 성능 테스트 중에는 대상 게임의 FPS와 프레임 타임에 주는 영향을 최소화한다.
- 시스템 소리와 마이크를 선택적으로 녹음한다.
- 녹화 중 앱 또는 PC가 비정상 종료되어도 결과물을 최대한 복구할 수 있게 한다.

## 3. 제외 범위

- 게임 데이터 또는 리플레이 데이터 사용
- 게임 프로세스 메모리 읽기
- DLL 인젝션 및 게임 렌더링 함수 후킹
- 안티치트 우회
- Python 기반 실시간 프레임 처리
- GDI 기반 실시간 화면 녹화
- FFmpeg raw video 표준 입력을 이용한 기본 녹화 경로
- 실시간 녹화 중 매 프레임 전체 영상을 CPU 메모리로 복사하는 구조

## 4. 최종 기술 구성

| 영역 | 선택 기술 |
|---|---|
| 언어 | C++20 |
| 빌드 | CMake, CMake Presets, vcpkg |
| 컴파일러 | MSVC |
| UI | Dear ImGui, Win32, Direct3D 11 |
| 기본 캡처 | Windows Graphics Capture |
| 폴백 캡처 | DXGI Desktop Duplication |
| GPU 처리 | Direct3D 11 shader |
| 실시간 인코딩 | FFmpeg libavcodec |
| 컨테이너 기록 | FFmpeg libavformat |
| NVIDIA | H.264/HEVC/AV1 NVENC |
| Intel | H.264/HEVC/AV1 QSV |
| AMD | H.264/HEVC/AV1 AMF |
| 범용 폴백 | Media Foundation Hardware MFT, libx264 |
| 시스템 소리 | WASAPI loopback |
| 마이크 | WASAPI capture |
| 이미지 저장 | Windows Imaging Component |
| 오프라인 변환 | ffmpeg.exe |

## 5. 성능 설계 원칙

목표 영상 경로는 다음과 같다.

```text
Windows 화면
    -> D3D11 texture
    -> GPU crop/scale/color conversion
    -> FFmpeg hardware frame
    -> NVENC/QSV/AMF
    -> encoded packet
    -> FFmpeg muxer
    -> MKV/MP4
```

다음과 같은 경로는 사용하지 않는다.

```text
D3D11 texture
    -> CPU raw frame
    -> ffmpeg.exe stdin
    -> GPU 재업로드
    -> NVENC
```

후자의 경로는 NVENC를 사용하더라도 GPU 다운로드, 프로세스 간 복사 및 GPU 재업로드가 발생하기 때문이다.

추가 원칙은 다음과 같다.

- 게임과 캡처에 사용하는 GPU 어댑터를 가능한 한 일치시킨다.
- 캡처 콜백에서는 대기, 파일 쓰기 또는 동기 인코딩을 수행하지 않는다.
- 캡처, GPU 처리, 영상 인코딩, 오디오, muxing을 별도 실행 단계로 분리한다.
- 프레임 큐는 크기가 제한된 ring buffer로 구현한다.
- 처리 지연이 누적되면 게임을 방해하지 않도록 오래된 영상 프레임을 드롭한다.
- 오디오는 가능한 한 드롭하지 않고 영상 타임라인과 동기화한다.
- UI 미리보기는 기본적으로 끄고, 활성화 시 15fps 이하로 제한한다.
- 녹화 중 UI 렌더링 빈도를 낮춘다.
- 저부하 녹화 모드에서는 미리보기, 불필요한 UI 애니메이션 및 비필수 통계를 끄고 하드웨어 인코더를 우선한다.
- 게임 성능 테스트 시 녹화 전 기준 구간과 녹화 중 구간의 평균 FPS, 1% low 및 프레임 타임을 같은 조건에서 비교한다.

## 6. 통합 캡처 대상 모델

```cpp
enum class CaptureTargetType
{
    Window,
    Region,
    Monitor
};

struct CaptureTarget
{
    CaptureTargetType type{};
    HWND window{};
    HMONITOR monitor{};
    RECT region{};
};

enum class RegionAnchorType
{
    VirtualDesktop,
    WindowClient
};

struct CaptureRegionPreset
{
    std::string id;
    std::string name;
    RegionAnchorType anchorType{};
    RECT region{};
    std::wstring processName;
    std::wstring windowTitleHint;
    SIZE referenceClientSize{};
};
```

녹화, GIF 및 스크린샷은 동일한 `CaptureTarget`을 사용한다.

```cpp
recorder.Start(target, recordingOptions);
gifRecorder.Start(target, gifOptions);
screenshot.Capture(target, imageOptions);
```

### 6.1 창 선택

- 현재 표시된 일반 최상위 창을 열거한다.
- 창 제목, 프로세스 이름 및 아이콘을 표시한다.
- 게임, 브라우저, 영상 플레이어 등 애플리케이션 종류에 따른 분기를 두지 않는다.
- 선택한 창이 종료되면 녹화를 안전하게 중지한다.
- 창 크기 변경 시 캡처 프레임 풀과 GPU 리소스를 재생성한다.
- 최소화된 창을 캡처할 수 없는 경우 사용자에게 상태를 안내한다.

### 6.2 영역 선택

- 전체 가상 데스크톱에 선택 오버레이를 표시하되 바탕 화면과 대상 내용을 식별할 수 있는
  밝기를 유지한다.
- 선택 사각형 바깥만 어둡게 하고 선택 안쪽은 투명하게 유지한다. 바깥 어둡기 기본값은
  30%로 하고 0~70% 범위에서 조절할 수 있게 한다.
- 영역 선택 오버레이의 어둡기와 선 모양은 상시 대상 테두리의 표시·굵기·불투명도 설정과
  분리한다. 두 기능은 목적과 수명이 다르므로 한 설정을 공유하지 않는다.
- 마우스 드래그로 영역을 선택한다.
- 선택선은 혼합 DPI에서도 식별 가능한 2~3 물리 픽셀의 고대비 이중선으로 표시한다.
- 좌표와 크기를 실시간 표시하고, 크기 안내는 선택 사각형 또는 현재 포인터가 있는 모니터
  안에서 잘리지 않게 배치한다.
- 사용 방법은 가상 데스크톱의 한쪽 모서리에만 두지 않고 각 모니터 또는 현재 선택 근처에
  표시한다.
- 방향키로 1픽셀, Shift+방향키로 10픽셀 조정한다.
- 다중 모니터와 서로 다른 DPI 배율을 처리한다.
- 현재 WGC 영역 캡처는 한 모니터의 프레임을 기준으로 자르므로 선택 시작 모니터의 경계를
  넘지 않게 제한하고 그 이유를 표시한다. 여러 모니터를 가로지르는 영역은 별도 GPU 합성
  기능을 구현한 뒤 허용한다.
- 마지막 선택 영역을 저장하고 다시 사용할 수 있게 한다.
- 자주 사용하는 영역을 사용자 지정 이름으로 여러 개 저장, 수정, 복제 및 삭제할 수 있게 한다.
- 프리셋은 가상 데스크톱 절대 좌표 또는 특정 창의 클라이언트 영역 기준 상대 좌표로 저장한다.
- 브라우저 창이 이동하거나 크기가 바뀌어도 창 기준 프리셋은 현재 클라이언트 좌표에 맞춰 복원한다.
- 창 기준 프리셋의 대상 창을 찾지 못하면 자동 실행하지 않고 창 재선택을 안내한다.
- 프리셋의 `Apply`는 저장 좌표를 즉시 적용한다. 미세 조정이 필요할 때만 별도의 `Edit region`
  동작으로 선택 오버레이를 연다.
- 영역 선택 중에는 기존 상시 대상 테두리를 잠시 숨겨 이전 대상과 새 선택을 혼동하지 않게
  하고, 취소하면 이전 대상과 테두리를 그대로 복원한다.
- 선택 영역은 CPU가 아닌 D3D11 shader로 자른다.

영역 선택 완료 기준:

- 기본 설정에서 선택 영역 내부의 영상과 작은 글자를 육안으로 구분할 수 있다.
- 바깥 어둡기 0%, 30%, 70%에서 선택선과 크기 안내를 모두 읽을 수 있다.
- 100%, 125%, 150%, 200% 혼합 DPI와 음수 좌표 모니터에서 마우스 좌표와 최종 캡처
  좌표 오차가 2픽셀 이내다.
- 한 모니터를 넘는 드래그는 잘린 결과를 만들지 않고 경계 제한 또는 명확한 오류로 처리된다.
- Escape 취소, Enter 확정, 방향키 조정 및 반복 실행 후 메인 앱과 전역 단축키 상태가
  정상적으로 복원된다.

현재 구현 상태 (2026-07-27):

- 기존의 전체 검정 레이어와 59% 전역 불투명도를 제거하고, 선택 사각형 바깥의 위·아래·
  왼쪽·오른쪽에만 네 개의 경량 layered window를 배치한다. 선택 내부는 실제 화면 밝기를
  유지한다.
- 바깥 어둡기를 0~70%로 조절하고 기본값 30%를
  `%LOCALAPPDATA%\OpenCapture\selection_overlay_settings.txt`에 원자적으로 저장한다.
- 불투명한 흰색·파란색 이중 선택선, 크기 안내 및 각 모니터의 조작 안내를 별도 클릭 통과
  시각 레이어로 표시한다.
- 드래그는 시작한 모니터 경계에서 제한되며 방향키 이동도 같은 경계를 넘지 않는다.
  경계 계산은 core 단위 테스트로 검증한다.
- 선택 중에는 이전 상시 대상 테두리를 숨기고 전역 단축키 요청을 버리며, 완료·취소 후
  메인 렌더 루프에서 현재 대상 테두리를 복원한다.
- Debug CTest, Debug/Release 빌드와 Release 프로세스 실행 유지를 검증했다.
- 실제 100~200% 혼합 DPI, 여러 모니터 배치 및 0%/30%/70% 밝기 체감 확인은 수동
  실기 검증이 남아 있다.

### 6.3 모니터 전체

- 연결된 모니터 목록과 주 모니터를 표시한다.
- 해상도, 주사율 및 HDR 여부를 표시한다.
- 모니터가 분리되면 녹화를 안전하게 종료한다.
- Windows Graphics Capture 실패 시 Desktop Duplication으로 폴백한다.

## 7. 인코더 정책

실행 시 사용 가능한 GPU와 FFmpeg encoder를 검사해 다음 순서로 자동 선택한다.

1. NVIDIA NVENC
2. Intel Quick Sync Video
3. AMD Advanced Media Framework
4. Media Foundation Hardware MFT
5. FFmpeg libx264

NVIDIA 기본 설정은 다음과 같다.

```text
기본 코덱:       H.264 NVENC
고급 코덱:       HEVC NVENC, AV1 NVENC
기본 픽셀 형식:  NV12
HDR 픽셀 형식:   P010
기본 프레임률:   60fps
키프레임 간격:   2초
품질 제어:       CQP 또는 VBR
기본 컨테이너:   MKV
```

실시간 녹화에서는 FFmpeg 라이브러리를 프로세스 안에서 사용한다. `ffmpeg.exe`는 파일 변환, remux 및 GIF 후처리에 사용한다.

## 8. 오디오 설계

```text
시스템 소리 -> WASAPI loopback --+
                                  +-> mixer -> AAC/Opus -> muxer
마이크 ------> WASAPI capture ----+
```

- 내부 오디오 포맷은 48kHz float PCM으로 통일한다.
- 시스템 소리와 마이크에 각각 볼륨 및 음소거를 제공한다.
- event-driven WASAPI shared mode를 사용한다.
- `IAudioClock`과 `QueryPerformanceCounter`를 이용해 타임스탬프를 연결한다.
- 장시간 녹화 시 오디오 resampling으로 드리프트를 보정한다.
- 녹화 중 장치가 분리되더라도 앱 전체가 종료되지 않도록 한다.

## 9. 파일 형식 및 안전성

기본 녹화는 MKV로 기록하고, 완료 후 사용자가 원하면 재인코딩 없이 MP4로 remux한다.

제공 모드는 다음과 같다.

- 안전 녹화: MKV 기록 후 MP4 자동 remux
- 직접 MP4: MP4로 바로 기록
- 원본 MKV: MKV 그대로 보관

추가 안전 기능은 다음과 같다.

- 녹화 중 임시 파일명 사용
- 정상 중지 후 최종 파일명 확정
- 녹화 시작 전 저장 공간 확인
- 디스크 쓰기 오류 감지 및 안전 종료
- 시작 시 복구 가능한 임시 파일 검색
- 동일 파일명 충돌 방지

## 10. UI 계획

UI는 하나의 간단한 메인 패널로 구성한다.

```text
+---------------------------------------+
| OpenCapture                           |
+---------------------------------------+
| 대상  (o) 창  ( ) 영역  ( ) 모니터  |
|       [ 캡처 대상 선택 ]             |
| 영역  [YouTube 영상 v] [저장/관리]  |
|                                       |
| 형식  [MP4/H.264 v]   FPS [60 v]     |
| 품질  [균형 v]        크기 [원본 v]  |
|                                       |
| [x] 시스템 소리 [ ] 마이크 [x] 커서 |
|                                       |
| 출력 폴더 [C:\...\OpenCapture] [변경]|
|                                       |
| 스크린샷                              |
| [클립보드만] [PNG 저장] [저장+복사]  |
|                                       |
| 비디오                                |
| [비디오 녹화 시작] [GIF 준비 중]     |
+---------------------------------------+
| 준비됨 / NVENC / 저장 위치           |
+---------------------------------------+
```

UI 코드는 상태 표시와 명령 전달만 담당한다.

행동 버튼은 결과를 바로 알 수 있는 동사와 대상을 함께 표시한다. 파일을 만들지 않는
클립보드 전용 캡처, PNG 파일 저장, 저장 후 복사를 명확히 구분하고, 아직 구현되지 않은
GIF처럼 눌러도 동작하지 않는 기능은 비활성 상태와 설명을 표시한다. 스크린샷과 녹화는
사용자가 지정한 하나의 공용 출력 폴더를 사용한다.

```cpp
void MainPanel::Draw()
{
    DrawTargetSelector(state.target);
    DrawVideoOptions(state.video);
    DrawAudioOptions(state.audio);
    DrawActionButtons(state);
    DrawStatus(state.status);
}
```

Win32 메시지 처리와 Dear ImGui/D3D11 초기화는 `platform` 모듈에 격리한다. 캡처와 인코딩 모듈은 Dear ImGui에 의존하지 않는다.

### 10.1 캡처 대상 상시 테두리

대상을 선택한 뒤에는 앱 창의 표시 여부와 관계없이 선택 영역을 항상 확인할 수 있어야 한다.
OpenCapture가 직접 만드는 비활성 오버레이 창을 대상 위에 표시하며, 이 창은 항상 위에 있지만
마우스 입력과 키보드 포커스를 가로채지 않고 작업 표시줄과 Alt+Tab에도 나타나지 않는다.

대상과 캡처 상태는 같은 위치의 테두리 색상으로 구분한다.

| 상태 | 표시 |
|---|---|
| 선택됨, 대기 중 | 얇은 파란색 테두리 |
| 스크린샷 캡처 중 | 노란색 테두리를 최소 400ms 표시 |
| 비디오 또는 GIF 녹화 중 | 노란색 테두리 유지 |
| 일시정지 | 주황색 테두리 |
| 대상 소실 또는 오류 | 빨간색 테두리 후 상태 안내 |

- 영역 대상은 선택한 사각형에만 테두리를 표시한다.
- 창 대상은 창 이동, 크기 변경, 최대화 및 DPI 변경을 따라간다. 최소화, 닫힘 또는
  가상 데스크톱 이동으로 보이지 않으면 테두리도 숨기고 상태를 갱신한다.
- 모니터 대상은 해당 모니터의 실제 캡처 영역 경계에 표시한다.
- 다중 모니터의 음수 좌표와 100%, 125%, 150%, 200% 배율을 처리한다.
- 테두리 창에는 `WDA_EXCLUDEFROMCAPTURE`를 적용해 스크린샷, 비디오 및 GIF 결과에
  테두리 자체가 들어가지 않게 한다. 지원하지 않는 환경에서는 캡처 직전 숨김 등
  안전한 폴백을 사용한다.

현재 영역 캡처는 Windows Graphics Capture로 모니터 전체를 받은 뒤 GPU에서 잘라낸다.
따라서 운영체제가 그리는 기본 캡처 테두리는 영역이 아니라 모니터 전체를 둘러싼다.
지원되는 Windows에서는 `GraphicsCaptureAccessKind::Borderless` 사용자 동의를 받은 뒤
`GraphicsCaptureSession::IsBorderRequired(false)`로 기본 테두리를 끄고 OpenCapture
오버레이만 표시한다. 권한이 거부되거나 API를 지원하지 않으면 운영체제 테두리를 강제로
없애지 않으며, 영역 테두리와 함께 전체 모니터 테두리가 보일 수 있다는 안내를 UI에
표시한다.

### 10.2 툴팁 원칙

처음 사용하는 사람도 결과를 예상할 수 있도록 모든 동작 버튼과 주요 설정에 툴팁을
제공한다.

- 스크린샷 버튼은 파일 생성 여부와 클립보드 복사 여부를 설명한다.
- 비디오 및 GIF 버튼은 시작/중지 동작, 출력 형식, 현재 단축키를 설명한다.
- FPS, 해상도, 품질, GIF 색상 수는 성능·화질·파일 크기에 미치는 영향을 설명한다.
- 대상, 영역 프리셋, 오디오, 커서, 출력 폴더 및 복구/변환 설정에도 설명을 제공한다.
- 비활성 컨트롤도 비활성 이유와 해결 방법을 툴팁으로 확인할 수 있게 한다.
- 마우스 호버뿐 아니라 키보드 포커스에서도 내용을 확인할 수 있게 하고, 툴팁은
  짧은 요약과 필요한 경우에만 한 줄의 주의사항으로 제한한다.

### 10.3 적응형 UI 배율

- 프로세스는 Per-Monitor V2 DPI awareness를 사용한다.
- 메인 창이 100%, 125%, 150%, 175%, 200% 배율의 모니터로 이동하면 `WM_DPICHANGED`와
  `GetDpiForWindow`로 현재 DPI를 갱신한다.
- Dear ImGui의 고정 픽셀 스타일을 매번 기본 스타일에서 다시 계산하고, 간격·패딩·
  스크롤바와 글꼴 배율에 현재 모니터 DPI를 함께 적용한다. 기존 스타일에 반복 곱하지 않는다.
- 사용자는 Windows 배율 위에 75~200% 추가 UI 배율을 적용할 수 있다. 기본값 100%는
  Windows 설정을 그대로 따르는 자동 모드다.
- 추가 UI 배율은 `%LOCALAPPDATA%\OpenCapture\ui_settings.txt`에 원자적으로 저장한다.
- 초기 창 크기는 유효 배율에 맞추되 현재 모니터 작업 영역의 90%를 넘지 않게 제한한다.
- UI에는 Windows 배율과 최종 유효 배율을 함께 표시해 중복 확대 여부를 알 수 있게 한다.

현재 구현 상태 (2026-07-27):

- 모니터 DPI와 사용자 보정 배율을 곱하는 순수 계산 모델과 75~200% 범위 단위 테스트를
  추가했다.
- 시작 모니터의 DPI로 초기 창, ImGui 스타일과 글꼴을 확대하고 모니터 이동 시
  `WM_DPICHANGED` 권장 창 위치·크기를 적용한다.
- 렌더 루프에서도 창 DPI를 확인해 누락된 DPI 알림을 보정한다.
- UI에서 Windows 배율, 유효 배율, 추가 배율을 확인·적용·초기화할 수 있다.
- Debug/Release 빌드와 CTest는 통과했다. 실제 4K 150%/200% 모니터 이동 시 글꼴
  선명도와 레이아웃 체감 검증은 남아 있다.

### 10.4 스크린샷 단축키, Quick Capture 및 UI 재구성

기능이 늘어나도 사용자가 가장 자주 하는 작업은 `대상 선택 → 캡처/녹화`여야 한다.
고정 대상 스크린샷, 영구 Region 변경, 일회성 Quick Capture를 서로 다른 명령과 상태로
분리하고, 메인 패널은 작업별 화면과 설정 화면을 분리한다.

#### 현재 구조와 개선 경계

| 항목 | 현재 상태 | 개선 방향 |
|---|---|---|
| 스크린샷 단축키 | `ScreenshotClipboard`가 클립보드 전용으로 고정됨 | 하나의 스크린샷 단축키와 별도의 출력 방식 설정 사용 |
| 일반 Region 선택 | 선택 오버레이가 열려도 메인 창이 화면에 남음 | 선택 직전 메인 창과 대상 테두리를 숨기고 종료 후 상태 복원 |
| Quick Capture | 임시 영역 모델과 명령이 없음 | 영구 대상을 변경하지 않는 일회성 영역 선택·캡처 경로 추가 |
| 메인 UI | 모든 기능이 하나의 긴 패널에 순서대로 표시됨 | Capture, Video, GIF, Settings 탭과 공통 상태 영역으로 재구성 |
| UI 상태 | `MainPanel::Draw`의 여러 함수 정적 변수에 분산됨 | 명시적인 `MainPanelState`와 섹션별 그리기 함수로 이동 |

#### 스크린샷 단축키 출력 방식

- 전역 스크린샷 단축키는 하나만 유지하고 출력 방식을 다음 세 값 중에서 선택한다.
  - `Clipboard`: 파일을 만들지 않고 클립보드에만 복사
  - `File`: 공용 출력 폴더에 PNG만 저장
  - `FileAndClipboard`: PNG 저장 후 클립보드에도 복사
- UI의 즉시 실행 버튼은 계속 각 결과를 명시적으로 실행할 수 있게 하고, 위 설정은
  전역 스크린샷 단축키와 Quick Capture 완료 시에만 적용한다.
- 설정은 `%LOCALAPPDATA%\OpenCapture\screenshot_settings.txt`에
  `shortcut_destination=clipboard|file|file_and_clipboard` 형식으로 원자적으로 저장한다.
- 기존 설정 파일이 없거나 값이 손상되면 현재 동작과 호환되는 `Clipboard`로 복구한다.
- `HotkeyAction::ScreenshotClipboard`는 저장 파일 인덱스 호환성을 유지하면서 표시 이름과
  내부 의미를 `Screenshot`으로 일반화한다. 기존 `hotkeys.txt`의 단축키는 그대로 유지한다.
- UI에는 `스크린샷 단축키 결과` 선택기와 실제 결과 문장을 함께 표시한다. 예:
  `Ctrl+Shift+F9 → PNG 저장 + 클립보드 복사`.

#### Region 선택 중 메인 창 숨김

- 일반 Region 선택을 시작하기 직전에 메인 창의 표시 여부, 최소화/최대화 상태,
  `WINDOWPLACEMENT` 및 이전 포커스 상태를 저장한다.
- 메인 창과 상시 대상 테두리를 숨긴 뒤 DWM에 숨김 상태가 반영된 다음 영역 선택
  오버레이를 표시해 OpenCapture 자체가 선택 화면을 가리지 않게 한다.
- 확정, Escape 취소, 오버레이 생성 실패 및 예외 경로 모두 같은 복원 함수를 사용한다.
- 메인 UI에서 시작한 일반 Region 선택은 종료 후 원래 위치와 표시 상태로 돌아온다.
  백그라운드 단축키로 시작해 원래 숨겨져 있던 창은 종료 후에도 숨김 상태를 유지한다.
- 영역 선택 중에는 중복 선택, 스크린샷, 녹화 시작 및 GIF 시작 단축키를 무시한다.
  단, 이미 진행 중인 녹화의 안전한 중지 정책은 상태 머신에서 별도로 처리한다.
- 취소 시 기존 `CaptureTarget`과 상시 테두리를 그대로 복원하고 설정 파일을 쓰지 않는다.
  확정 시에만 새 영구 Region을 저장하고 해당 영역 테두리를 다시 표시한다.

#### Quick Capture

Quick Capture는 현재 선택된 창·모니터·Region 및 저장된 프리셋을 변경하지 않는 일회성
스크린샷이다.

```cpp
enum class ScreenshotRequestOrigin
{
    MainPanel,
    SelectedTargetHotkey,
    QuickCapture,
};

struct ScreenshotRequest
{
    ScreenshotDestination destination{};
    CaptureTarget target{};
    ScreenshotRequestOrigin origin{};
};

std::optional<CaptureTarget> PickTemporaryRegion(HWND owner);
```

- 기본 단축키 후보는 기존 F9~F11과 겹치지 않는 `Ctrl+Shift+F8`이며 설정 화면에서 변경한다.
- 단축키를 누르면 메인 창을 앞으로 가져오지 않고 영역 선택 오버레이만 표시한다.
- 사용자가 드래그 후 Enter를 누르면 임시 `CaptureTarget`으로 한 프레임을 캡처하고,
  `스크린샷 단축키 결과` 설정에 따라 클립보드, PNG 또는 둘 다로 출력한다.
- Escape를 누르면 아무 파일이나 클립보드 변경 없이 종료한다.
- `PickTemporaryRegion`은 좌표와 모니터만 반환하며 `CaptureTargetPicker::selected_`,
  `settings.ini`, Region 프리셋 및 상시 대상 테두리를 변경하지 않는다.
- `StartScreenshot`은 항상 `ScreenshotRequest`를 받아 선택 대상과 임시 대상을 같은
  GPU crop/WIC/클립보드 경로로 처리한다. 임시 캡처를 위해 별도의 CPU 전체 화면 복사
  경로를 만들지 않는다.
- Quick Capture가 캡처 세션을 임시로 소유한 경우 완료·실패·취소 후 반드시 세션과
  오버레이를 정리한다. 기존 고정 대상과 UI 상태는 그대로 남긴다.
- 첫 구현에서는 녹화·일시정지·GIF 녹화·GIF 변환·remux·다른 스크린샷 처리 중에는
  Quick Capture를 시작하지 않고 정확한 거부 이유를 상태 영역에 표시한다. 녹화와
  동시에 다른 영역을 캡처하는 기능은 두 번째 WGC 세션과 부하 검증이 필요하므로 분리한다.

#### 메인 UI 정보 구조

```text
+----------------------------------------------------------------+
| OpenCapture | 대상: YouTube Region | 출력: C:\...\OpenCapture |
| [Quick Capture]                         상태: 준비됨 / NVENC    |
+----------------------------------------------------------------+
| Capture | Video | GIF | Settings                                |
+----------------------------------------------------------------+
| 선택한 탭의 핵심 작업                                           |
| - Capture: 대상, Region 프리셋, 스크린샷 결과와 실행            |
| - Video: 녹화 시작/중지, 품질, FPS, 오디오, 인코더              |
| - GIF: GIF 시작/중지, 해상도, FPS, 색상, 용량 안전 제한         |
| - Settings: 출력, 단축키, 디스플레이, 테두리, 영역 선택, 고급   |
+----------------------------------------------------------------+
| 최근 결과 또는 오류 | 현재 단축키 | 캡처/변환 진행 상태         |
+----------------------------------------------------------------+
```

- 가장 자주 쓰는 현재 대상, 출력 폴더, Quick Capture 및 상태는 탭과 관계없이 상단에 둔다.
- Capture 탭은 `대상 선택`, `Region 프리셋`, `스크린샷` 세 카드만 기본으로 펼친다.
- Video와 GIF 설정을 분리해 GIF 해상도·색상 수가 일반 녹화 옵션과 섞이지 않게 한다.
- Settings 탭은 `일반/출력`, `단축키`, `디스플레이`, `테두리/선택 화면`,
  `고급/진단`의 접이식 그룹을 사용한다.
- 복구 파일, remux, 인코더 진단처럼 일상적으로 쓰지 않는 기능은 `고급/진단` 아래에 둔다.
- 모든 실행 버튼은 동작 결과를 이름에 포함하고, 비활성 상태에서는 이유를 툴팁과
  공통 상태 영역에 표시한다.
- 창 폭이 좁거나 150~200% DPI이면 같은 줄의 카드와 버튼을 세로로 배치해 가로 잘림을
  만들지 않는다. 고정 픽셀 폭은 최소화하고 `GetContentRegionAvail()` 기준으로 배치한다.
- `MainPanel::Draw`는 `DrawHeader`, `DrawCaptureTab`, `DrawVideoTab`, `DrawGifTab`,
  `DrawSettingsTab`, `DrawStatusBar`로 분리하고 설정 편집 상태는 `MainPanelState`가 소유한다.

#### Dear ImGui 사용 판단

현재 요구사항은 Dear ImGui가 제공하는 탭, 자식 영역, 접이식 헤더, 테이블, 툴팁,
키보드 탐색 및 DPI 배율로 충분히 구현할 수 있다. UI를 정돈하기 위해 라이브러리를
교체할 필요는 없다. 기존 D3D11 렌더 루프와 잘 맞고 추가 런타임 및 별도 UI 스레드를
도입하지 않는 장점도 유지한다.

다만 Dear ImGui는 Windows 네이티브 UI Automation, 스크린 리더, 고급 접근성 및
네이티브 설정 앱 모양에서 한계가 있다. 향후 회사 배포에서 WCAG 수준 접근성이나
네이티브 컨트롤이 필수 요구가 되면 메인 캡처 패널은 유지하고 설정 창만 WinUI 3로
분리하는 하이브리드 방식을 검토한다. 이번 단계에서는 UI 프레임워크 교체를 범위에
포함하지 않는다.

## 11. 권장 프로젝트 구조

```text
OpenCapture/
|-- CMakeLists.txt
|-- CMakePresets.json
|-- vcpkg.json
|-- README.md
|-- LICENSE
|-- app/
|-- core/
|-- capture/
|-- graphics/
|-- audio/
|-- encoding/
|-- image/
|-- ui/
|-- platform/
`-- tests/
```

모듈 책임은 다음과 같다.

- `app`: 프로그램 진입점과 전체 구성
- `core`: 녹화 세션, 상태 머신, 설정 및 타임스탬프
- `capture`: WGC와 Desktop Duplication
- `graphics`: crop, scale, 색 공간 변환 및 HDR 처리
- `audio`: WASAPI 캡처, 믹싱 및 오디오 클록
- `encoding`: FFmpeg encoder, muxer 및 하드웨어 탐색
- `image`: 스크린샷 저장과 클립보드 처리
- `ui`: Dear ImGui 패널
- `platform`: Win32, D3D11, 트레이, 단축키 및 DPI 처리
- `tests`: 기능, 장시간 및 성능 테스트

## 12. 단계별 구현 계획

### 12.0 현재 기준선과 실행 우선순위

2026-07-27 점검 기준으로 일상적인 스크린샷, 클립보드 캡처, H.264/AAC 녹화,
영역 프리셋 및 용량 제한 GIF 흐름은 실제 UI에 연결되었다. 상시 대상 테두리와 일부 전역
단축키도 동작한다. 영역 선택 내부 밝기와 단일 모니터 경계 문제는 구현상 해결했으며
혼합 DPI 실기 검증, 표시만 되고 동작하지 않는 옵션, 저부하 실측 및 배포 준비가 남아 있다.

현재 구현된 기준선:

- CMake, Ninja, MSVC 및 vcpkg 기반 Debug/Release 빌드
- Win32/D3D11/Dear ImGui 애플리케이션
- 창, 모니터 및 화면 영역 선택과 이름이 있는 영역 프리셋
- Windows Graphics Capture와 QPC 프레임 타임스탬프
- D3D11 shader crop, scale 및 BGRA-to-NV12 변환
- H.264 NVENC, MKV muxing, 시작/중지 및 실제 활성 인코더 표시
- 인코더 자동/수동 선택과 OpenH264 최종 폴백
- 선택 대상 PNG 저장과 디스크 없는 `CF_DIBV5` 클립보드 캡처
- WASAPI 시스템·마이크 캡처, 48kHz stereo 믹싱 및 AAC MKV 오디오 트랙
- 스크린샷과 녹화가 함께 사용하는 사용자 지정 출력 폴더
- `.part.mkv` 안전 저장, 시작 전 공간 검사, 미완료 파일 검색 및 검증 후 복구
- 정지 구간을 영상·오디오 타임라인에서 제거하는 녹화 일시정지/재개
- H.264/AAC MP4 무재인코딩 remux 성공 후 원본 MKV 제거
- 게임 성능(1080p60 H.264)·균형(1080p60 HEVC)·용량 절약(1080p30 HEVC)·직접 설정 녹화 프로필
- Temporal AQ와 lookahead를 배제한 실시간 안전 Efficient 모드, 8개 GPU 출력 텍스처 풀
- 처리 적체 시 오래된 프레임을 버리고 최신 프레임을 우선하며 실시간 드롭·인코더 제출 지연을 UI에 표시
- GPU에서 종횡비를 유지해 축소한 저FPS 소스와 FFmpeg 2-pass 팔레트를 사용하는 GIF
- GIF 360p/480p/720p/1080p, 6/10/12/15/20/30fps, 64/128/192/256색 설정
- 30초 또는 5억 픽셀 처리 예산의 GIF 자동 종료와 실패 시 소스 MKV 보존
- GIF 변환 작업 스레드, 2-pass 단계별 진행률, 취소 및 앱 종료 시 안전한 정리
- 영역·창·모니터의 상시 대상 테두리와 표시 여부, 굵기 및 불투명도 설정
- 주요 기능 툴팁과 클립보드 캡처, 비디오 및 GIF의 사용자 지정 전역 단축키
- 선택 오버레이를 다시 열지 않는 영역 프리셋 즉시 적용
- 선택 내부를 밝게 유지하는 외부 전용 dimming, 0~70% 설정 및 시작 모니터 경계 제한
- 현재 모니터 DPI 자동 반영과 75~200% 사용자 보정을 제공하는 적응형 ImGui UI
- WGC 원본 프레임 시각, UI VSync와 분리된 실시간 펌프, 지연 시 무제한 중복 인코딩을
  방지하는 단일 PTS 점프 및 source/skipped-tick 진단 표시
- 압축 패킷을 2,048개 제한 큐로 전달하는 비동기 mux writer, 종료 시 drain/trailer 순서,
  frame-ready event 대기 및 source FPS/최대 gap/WGC drop/mux peak 진단 표시

다음 항목은 아직 실제 기능과 연결되지 않았다.

- 커서 포함 여부는 WGC 기본 동작으로 항상 포함되며, 설정은 설명과 함께 비활성화했다.
- HEVC 형식 선택과 저부하 모드는 실제 연결 전까지 UI에 노출하지 않는다.

중간 진척도 추정:

- 핵심 개인 사용 흐름: 약 75~80%. 캡처, 저장, 클립보드, 녹화, GIF 및 프리셋은 사용할
  수 있으나 선택 UX와 일부 옵션의 신뢰성 보완이 필요하다.
- 여러 PC에 배포할 수 있는 완성도: 약 55~65%. GPU별 성능, 장시간 안정성, WGC 폴백,
  패키징·서명·라이선스 및 진단 기능은 검증 비중이 크다.
- 이 수치는 코드 줄 수가 아니라 남은 기능의 위험도와 실기 검증 범위를 가중한 추정치다.

향후 작업은 기존 단계 번호와 무관하게 다음 순서로 진행한다.

0. 영역 선택 가독성과 정확성을 보완한다. 선택 안쪽 밝기, 바깥 어둡기 설정, 고대비 선,
   모니터별 안내와 단일 모니터 경계 구현은 완료했다. 혼합 DPI 및 밝기 실기 검증이 남았다.
1. 스크린샷 단축키의 클립보드/PNG/둘 다 출력 설정과 공통 `ScreenshotRequest` 모델을
   구현한다.
2. 일반 Region 선택 중 메인 창 숨김·복원을 먼저 안정화한 뒤, 영구 대상을 바꾸지 않는
   Quick Capture와 전역 단축키를 구현한다.
3. 메인 UI를 Capture, Video, GIF, Settings 탭과 공통 상태 영역으로 재구성하고 고 DPI와
   좁은 창에서의 자동 배치를 검증한다.
4. 커서 설정은 실제 연결 전까지 비활성화하고 이유를 툴팁에 표시했다. HEVC·AV1 선택,
   원본/1080p/720p GPU 축소, 압축 효율 모드와 코덱 폴백은 실제 인코더 경로에 연결했다.
   코덱별 NVIDIA 실기 및 Intel·AMD 검증은 계속 수용 기준으로 관리한다.
5. 공식 Release 출력은 `build/repo-ninja-x64-release`로 통일한다. `release-verification`
   같은 임시 검증 폴더는 문서·VS Code·스크립트의 기본 경로로 사용하지 않고, 잠긴 실행
   파일을 검증할 때만 폐기 가능한 별도 staging 디렉터리를 사용한다.
6. 60fps 타이밍 안정화 1차 작업으로 WGC 원본 시각 사용, UI VSync 분리, 지연 후 무제한
   따라잡기 제거, frame-ready event 대기, 비동기 mux writer 및 성능 진단 표시를 완료했다.
   다음으로 GPU 프레임 처리·인코딩 작업 스레드 분리와 저부하 모드를 연결하고 NVIDIA,
   Intel, AMD 및 혼합 GPU에서
   1080p/1440p/4K, 게임 1% low, 장시간 A/V 동기화 합격 기준을 실측한다.
7. WGC 실패와 borderless 권한 제한을 처리할 Desktop Duplication 폴백을 구현하고,
   영역·모니터 테두리가 결과에 들어가지 않는 패키징 시나리오를 검증한다.
8. 오디오 장치 선택, 볼륨·음소거, Opus, 장치 변경 복구 및 2시간 드리프트 보정을 완성한다.
9. VP9/Opus와 MP4/MKV/WebM 범용 오프라인 변환, QSV/AMF를 완성한다.
10. 남은 단축키 동작, 트레이, 설정 통합·마이그레이션, 세션 로그와 진단 내보내기를 추가한다.
11. 자동 UI 회귀 시험, 배포 패키지, 의존성 고지, 서명 및 회사 PC 보안 안내를 완성한다.

각 마일스톤은 Debug/Release 빌드, 관련 자동 테스트 및 실제 결과물 검증을 완료해야 종료한다.

### 12.0.2 v0.2.0 배포 기준점

v0.2.0은 Quick Capture, 스크린샷 단축키 결과 설정, Region 선택 중 메인 창 숨김,
Capture/Video/GIF/Settings 탭, 적응형 DPI UI와 opt-in 알림 영역 상주를 포함하는
저부하 최적화 직전 기준점이다.

- 신규 사용자의 창 닫기 기본값은 완전 종료이며 저장된 opt-in 설정만 트레이에 상주한다.
- Windows 서비스, 자동 시작, 예약 작업, 관리자 권한, 프로세스/DLL 주입 및 게임 후킹을
  사용하지 않는 동작 범위를 한·영 README와 릴리스 노트에 명시한다.
- CMake, vcpkg manifest와 Windows EXE 버전을 0.2.0으로 일치시킨다.
- `scripts/package_release.ps1`로 런타임 DLL, 제3자 고지, FFmpeg 실제 빌드 옵션,
  소스 커밋, 서명 상태, 파일별 해시 및 최종 ZIP SHA-256을 재현한다.
- 변환 전 FPS gate, 백그라운드 ImGui/Present 중단과 BGRA NVENC 직접 입력은 조사 결과만
  보존하고 v0.2.0에 섞지 않는다. 이후 각각 독립 측정해 채택한다.

### 12.0.1 2026-07-27 중간 점검에서 발견한 문제와 개선 항목

| 우선순위 | 영역 | 현재 문제 | 개선 및 완료 조건 |
|---|---|---|---|
| P0 | 영역 선택 가독성 | 전체 화면을 어둡게 하던 문제는 수정했다. | 내부 투명, 바깥 기본 30%, 0~70% 설정과 고대비 선 구현 완료. 혼합 DPI 및 0%/30%/70% 실기 가독성 시험이 남았다. |
| P0 | 다중 모니터 영역 | 한 모니터 WGC 프레임 밖으로 드래그할 수 있던 문제는 단기 수정했다. | 선택 시작 모니터 경계 제한과 단위 테스트 완료. 장기 교차 모니터 GPU 합성은 별도 후속 기능이다. |
| P0 | 스크린샷 단축키 결과 | 현재 F9 단축키가 클립보드 전용으로 고정되어 있다. | 클립보드, PNG, 저장+복사 설정을 저장하고 기존 단축키 등록을 유지한 채 공통 요청 모델로 처리한다. |
| P0 | Region 선택 시 앱 가림 | 메인 OpenCapture 창이 선택 화면에 남아 뒤쪽 영역을 가린다. | 선택 전 창·테두리를 숨기고 확정·취소·실패 후 이전 창 상태와 영구 대상 테두리를 복원한다. |
| P0 | UI 신뢰성 | 커서 설정은 WGC 기본 동작과 연결되지 않았다. | 커서는 설명과 함께 비활성화했다. HEVC·AV1·저부하 설정은 실제 경로와 합성 스모크에 연결하고, 활성 인코더·출력 크기·비트레이트와 실패 폴백을 표시한다. |
| P1 | 선택 상태 충돌 | 영역 선택 중 이전 상시 테두리와 전역 단축키가 충돌하던 경로를 차단했다. | picker 진입 시 테두리 숨김과 일반 단축키 무시를 구현했다. 확정·취소 반복 수동 시험이 남았다. |
| P1 | Quick Capture | 일회성 영역 선택 경로가 없어 현재 Region을 바꾸지 않고 즉시 캡처할 수 없다. | 임시 대상 API, F8 기본 단축키, 출력 설정 연동과 작업 충돌 상태 머신을 구현한다. |
| P1 | UI 복잡도 | 대상, 녹화, GIF, 단축키와 진단이 하나의 긴 패널에 섞여 있다. | Capture/Video/GIF/Settings 탭, 공통 헤더·상태바와 고 DPI 반응형 배치로 재구성한다. |
| P1 | 성능 보장 | WGC 원본 시각, UI VSync 분리, 지연 후 무제한 중복 인코딩 제거, frame event 대기, 비동기 mux와 source FPS/gap/drop/mux peak 표시는 구현했다. GPU 처리·인코딩은 아직 메인 실시간 경로에 있으며 GPU별 장시간 실측도 부족하다. | D3D11 동시성 경계를 검증한 뒤 GPU 처리·인코딩 작업 스레드를 분리하고 게임 평균 FPS와 1% low, CPU/GPU 엔진별 사용률, 드롭, 큐 지연을 같은 장면의 전후 비교로 기록한다. |
| P1 | 캡처 폴백 | WGC 미지원·권한·드라이버 실패 시 사용할 Desktop Duplication이 없다. | 자동/WGC/DXGI 선택과 오류 폴백을 구현하고 기존 GPU 처리·인코더 경로를 재사용한다. |
| P1 | 회귀 시험 | 핵심 엔진 스모크는 있으나 영역 오버레이, 프리셋 즉시 적용, 테두리 및 전역 단축키의 UI 회귀 시험이 부족하다. | 반복 선택·취소·Enter, DPI 전환, 모니터 분리, 단축키 충돌 및 결과 좌표를 포함한 자동/수동 체크리스트를 버전 관리한다. |
| P2 | 설정 관리 | 출력 폴더, 마지막 대상, 프리셋, 테두리 및 단축키가 여러 텍스트 파일과 서로 다른 저장 방식으로 분산되어 있다. | 버전이 있는 단일 설정 모델, 원자적 저장, 손상 복구, 기존 파일 마이그레이션 및 기본값 초기화를 구현한다. |
| P2 | 진단 가능성 | 실패 원인과 성능 상태를 재현할 세션 로그 및 개인정보 제거 진단 내보내기가 아직 없다. | GPU·드라이버·캡처 방식·인코더·드롭·오류 코드를 기록하고 창 제목과 경로를 선택적으로 제거한다. |
| P2 | 오디오 완성도 | 기본 WASAPI/AAC 경로는 동작하지만 장치 선택, 개별 볼륨, Opus, 장치 변경 및 장시간 드리프트 보정이 남았다. | 2시간 A/V 오차 50ms 이내, 장치 분리 시 앱 생존 및 명확한 상태 표시를 확인한다. |
| P2 | 빌드 위생 | 임시 검증용 `release-verification` 경로가 공식 산출물 경로처럼 남아 혼동을 만들었다. | preset의 공식 Release 경로만 사용자 작업 경로로 문서화하고 임시 staging은 정리 가능한 용도로 격리한다. |
| P3 | 배포 신뢰 | 패키징된 borderless 권한, 서명, FFmpeg/코덱 고지와 회사 PC 보안 안내가 완료되지 않았다. | 재현 가능한 패키지, 해시, 의존성 고지, 서명 상태 및 보안팀 예외 요청에 필요한 기술 설명을 릴리스에 포함한다. |

### 12.1 1단계: C++ 프로젝트 기반

작업:

- CMake 기반 C++20 프로젝트를 생성한다.
- MSVC, Windows SDK 및 x64 Release 구성을 설정한다.
- vcpkg manifest를 추가한다.
- Dear ImGui Win32/DX11 backend를 연결한다.
- Win32 창과 D3D11 device를 생성한다.
- FFmpeg 개발 라이브러리 탐색 구성을 추가한다.
- 프로젝트 README와 오픈소스 라이선스 고지를 작성한다.

완료 기준:

- x64 Debug/Release 빌드가 성공한다.
- 빈 메인 창이 실행되고 정상 종료된다.
- 사용 중인 D3D11 GPU와 FFmpeg 버전을 표시한다.
- UI와 엔진 모듈의 의존성이 분리되어 있다.

### 12.2 2단계: 통합 캡처 대상 선택

작업:

- `CaptureTarget`과 관련 옵션 모델을 구현한다.
- 모니터와 최상위 창을 열거한다.
- 창 제목, 프로세스 이름 및 아이콘을 표시한다.
- DPI aware 영역 선택 오버레이를 구현한다.
- 마지막 선택 대상을 설정 파일에 저장한다.
- 이름이 있는 영역 프리셋의 생성, 수정, 복제, 삭제 및 정렬을 구현한다.
- 화면 절대 좌표 프리셋과 창 클라이언트 기준 상대 좌표 프리셋을 구분한다.
- 브라우저 등 대상 창의 이동, 크기 및 DPI 변경 시 창 기준 프리셋을 현재 좌표로 변환한다.

완료 기준:

- 임의의 일반 Windows 창을 선택할 수 있다.
- 임의의 화면 영역과 모니터를 선택할 수 있다.
- 게임 전용 코드 분기가 없다.
- 동일 대상을 녹화와 스크린샷에 전달할 수 있다.
- 저장한 영역 프리셋을 앱 재시작 후 다시 선택할 수 있다.
- 창 기준 프리셋은 창 위치가 바뀐 뒤에도 동일한 콘텐츠 영역을 가리킨다.
- 프리셋의 대상 창을 찾지 못한 경우 잘못된 좌표를 캡처하지 않고 사용자 확인을 요청한다.

### 12.3 3단계: Windows Graphics Capture

작업:

- C++/WinRT를 초기화한다.
- D3D11 device로 `Direct3D11CaptureFramePool::CreateFreeThreaded`를 구성한다.
- 프레임을 `ID3D11Texture2D`로 수신한다.
- 창과 모니터 크기 변경을 처리한다.
- device lost와 캡처 대상 종료를 처리한다.
- 모든 프레임에 QPC 기반 타임스탬프를 부여한다.

완료 기준:

- 창, 영역 및 모니터를 동일한 캡처 엔진으로 처리한다.
- 1080p 60fps 프레임을 안정적으로 수집한다.
- 실시간 경로에서 전체 프레임 CPU 복사가 없다.
- 종료 후 모든 캡처 및 GPU 리소스가 해제된다.

### 12.4 4단계: GPU 영상 처리

작업:

- D3D11 shader 기반 crop과 scale을 구현한다.
- BGRA에서 NV12로 변환한다.
- H.264에 맞게 출력 크기를 짝수로 보정한다.
- 커서 포함 여부를 처리한다.
- HDR 감지, P010 및 HDR-to-SDR 톤매핑 기반을 마련한다.

완료 기준:

- 영역 녹화와 영역 스크린샷 좌표가 일치한다.
- 4K 입력을 GPU에서 1080p로 축소할 수 있다.
- CPU 기반 픽셀 변환을 사용하지 않는다.

### 12.5 5단계: FFmpeg NVENC

작업:

- libavcodec, libavformat 및 libavutil을 연결한다.
- FFmpeg hardware device와 NVENC encoder를 탐색한다.
- H.264 NVENC를 기본으로 구현한다.
- HEVC NVENC와 AV1 NVENC를 선택 옵션으로 추가한다.
- D3D11/CUDA 상호운용을 구성한다.
- GPU texture를 FFmpeg hardware frame으로 전달한다.
- MKV/MP4 muxer와 PTS/DTS 생성을 구현한다.
- encoder flush와 안전 종료를 처리한다.

완료 기준:

- NVIDIA 환경에서 NVENC가 자동 선택된다.
- 운영체제 GPU 모니터에서 Video Encode 사용을 확인한다.
- raw-frame stdin 파이프를 사용하지 않는다.
- 생성된 파일을 일반 플레이어와 ffprobe로 검증한다.

### 12.6 6단계: 다른 인코더 폴백

작업:

- Intel QSV와 AMD AMF를 탐색하고 연결한다.
- Media Foundation hardware MFT 폴백을 구현한다.
- libx264 최종 폴백을 구현한다.
- 실제 사용 중인 인코더를 UI와 로그에 표시한다.

완료 기준:

- NVIDIA가 없는 PC에서도 녹화할 수 있다.
- 사용할 수 없는 인코더는 자동으로 안전하게 대체된다.
- 자동 선택과 사용자 수동 선택을 모두 제공한다.

현재 구현 상태 (2026-07-24):

- H.264 후보를 정책 순서대로 실제 초기화해 보고 실패 시 다음 후보를 시도하는 자동 폴백을 연결했다.
- UI에서 자동 선택 또는 개별 H.264 인코더 수동 선택을 제공하고, 녹화 중 실제 활성 인코더를 표시한다.
- NVENC/AMF/Media Foundation의 D3D11 입력 경로와 OpenH264/libx264의 최종 CPU 호환 경로를 분리했다.
- OpenH264 폴백은 GPU NV12 staging readback 후 YUV420P로 변환하며, 하드웨어 인코더가 모두 실패한 경우에만 사용한다.
- 현재 NVIDIA 시험 PC에서 NVENC 자동 선택과 OpenH264 강제 폴백 MKV를 검증했다. Media Foundation은 등록은 확인됐지만 이 PC에서 초기화가 거부되어 OpenH264로 안전하게 넘어간다.
- Intel QSV 및 AMD AMF의 실제 하드웨어 경로는 해당 GPU가 있는 시험 환경에서 추가 검증이 필요하다.

### 12.7 7단계: WASAPI 오디오

작업:

- 시스템 소리 loopback과 마이크 capture를 구현한다.
- 장치 선택, 볼륨 및 음소거를 구현한다.
- 오디오 ring buffer와 mixer를 구현한다.
- AAC와 Opus 인코딩을 연결한다.
- 영상 타임라인과 동기화하고 장시간 드리프트를 보정한다.

완료 기준:

- 시스템 소리와 선택적 마이크가 영상에 포함된다.
- 한 시간 녹화 후 체감 가능한 싱크 어긋남이 없다.
- 오디오 장치 변경이나 분리로 앱 전체가 종료되지 않는다.

현재 구현 상태 (2026-07-24):

- 시스템 기본 출력의 WASAPI loopback과 기본 마이크 capture를 event-driven shared mode로 초기화하는 독립 모듈을 구현했다.
- 캡처 스레드, 종료 이벤트, 128패킷 제한 큐, drop-oldest 계수, 장치 mix format 및 WASAPI QPC 위치를 제공한다.
- 진단 도구에서 시스템 loopback 48kHz stereo float와 마이크 48kHz mono float 초기화를 확인했다.
- 0.5초 마이크 시험에서 51패킷, 드롭 0을 확인했다. 시스템 loopback은 시험 시 출력이 무음이어서 포맷 초기화만 확인했다.
- WASAPI float32/PCM16/PCM32 패킷을 48kHz stereo float로 정규화하고 QPC 위치를 영상 시작 시각 기준 sample PTS로 변환한다.
- 시스템 소리와 마이크를 50ms 지연 타임라인에서 믹싱하고 누락 구간은 무음으로 유지한다.
- UI의 시스템 소리와 마이크 옵션을 녹화 세션에 연결하고 FFmpeg AAC-LC 192kbps와 MKV 오디오 스트림을 구현했다.
- 합성 A/V 스모크에서 H.264 60fps와 AAC 48kHz stereo가 포함된 1.023초 MKV를 검증했다.
- 실제 WGC/WASAPI 스모크에서 1920x1080 H.264 63프레임과 AAC 49프레임을 포함한 1.066초 MKV를 검증했다.
- 장치 선택, 개별 볼륨·음소거, Opus, 장치 분리 복구 및 1시간/2시간 드리프트 검증은 남아 있다.

### 12.8 8단계: 녹화 제어와 안전한 저장

작업:

- 시작, 중지 및 일시정지 상태 머신을 구현한다.
- MKV 안전 녹화와 MP4 자동 remux를 구현한다.
- 저장 공간, 파일 충돌 및 디스크 오류를 처리한다.
- 비정상 종료 후 임시 파일 검색과 복구 안내를 구현한다.
- 게임 플레이 테스트용 저부하 녹화 모드를 구현한다.
- 저부하 모드에서는 미리보기와 비필수 UI 갱신을 끄고 캡처 및 인코딩 큐 지연을 우선 감시한다.

완료 기준:

- 반복 시작/중지에서 자원 누수가 없다.
- 일시정지 구간이 최종 타임라인에서 제거된다.
- 중지 후 파일이 정상적으로 재생된다.
- 실패 원인이 UI와 로그에 명확히 나타난다.
- 저부하 모드 활성 여부와 실제 하드웨어 인코더 사용 상태가 UI와 로그에 나타난다.
- 게임 성능이 임계치 이상 저하되면 프레임 드롭 또는 품질 조정 정책을 적용하고 경고한다.

현재 구현 상태 (2026-07-23):

- 최종 파일과 별도의 `이름.part.mkv`에 녹화하고 정상 trailer 및 파일 close 후 같은 볼륨에서 최종 `.mkv`로 rename한다.
- 녹화 시작 전에 출력 폴더의 사용 가능 공간을 확인하고 최소 512MiB 미만이면 시작하지 않는다.
- 최종 파일명 충돌 시 숫자 suffix를 사용하며 기존 최종 파일이나 미완료 파일을 덮어쓰지 않는다.
- 앱 시작 시 사용자 지정 출력 폴더의 `.part.mkv` 파일을 검색하고 파일별 `Recover` 동작을 UI에 표시한다.
- 복구 전에 FFmpeg로 컨테이너와 영상 스트림을 판독하며, 성공한 파일만 충돌 없는 최종 `.mkv` 이름으로 확정한다.
- 정상 종료 스모크에서 최종 H.264/AAC MKV만 남고 `.part.mkv`가 제거되는 것을 확인했다.
- 강제 실패 스모크에서 최종 파일은 생성되지 않고 ffprobe로 읽을 수 있는 H.264/AAC `.part.mkv`가 보존되는 것을 확인했다.
- 복구 스모크에서 정상 파일 확정, 기존 최종 파일 보존과 숫자 suffix 사용, 손상 파일 원본 보존을 확인했다.
- 일시정지 시 WASAPI를 중단하고 누적 정지 QPC를 영상 PTS와 오디오 샘플 위치에서 동일하게 제거한다.
- 0.5초 녹화, 0.5초 정지, 0.5초 재개 스모크 결과가 1.045초 H.264/AAC MKV로 생성되어 정지 구간 제거를 확인했다.
- MP4 선택 시 안전 MKV 확정 후 같은 폴더에 MP4를 자동 생성하며 수동 `Convert to MP4`도 제공한다.
- remux는 재인코딩 없이 H.264/AAC 패킷 타임스탬프만 MP4 time base로 변환하며, MP4가 성공적으로 확정된 뒤 원본 MKV를 제거한다. 실패 시에는 MKV를 보존한다.
- remux 정상·이름 충돌·손상 입력 스모크와 자동 MP4 녹화 스모크를 통과했다.
- 녹화 프로필은 게임 성능·균형·용량 절약·품질 우선 선택 시 FPS·코덱·최대 해상도·인코더 강도·fallback을 함께 확정한다.
- Efficient는 P5 압축을 유지하되 게임과 경쟁하는 Temporal AQ 및 20프레임 lookahead를 사용하지 않는다.
- 품질 우선은 원본 해상도 HEVC 60fps, P6, 제한된 8프레임 lookahead, 3 B-frame middle reference와 quarter-resolution multipass를 명시적으로 선택한다.
- Windows build 26100 이상은 녹화 FPS를 WGC `MinUpdateInterval`에 전달하고, quality lookahead·B-frame·bounded worker가 참조 중인 프레임의 조기 재사용을 막도록 NV12 출력 풀을 24개로 유지한다.
- 영상 `Send/Receive/Flush`는 4프레임 bounded worker가 전담한다. worker가 밀리면 가장 오래된 대기 프레임을 버리고 캡처/UI 스레드는 기다리지 않는다.
- UI의 GPU 압력은 encoder effort·codec·FPS·해상도 등급으로 계산한 사전 예측이며 실제 게임 benchmark로 표시하지 않는다.
- 시작 시 320x180 NV12로 실제 D3D11 encoder-open을 검증하고 Auto는 활성 GPU 하드웨어 AV1 → HEVC → H.264를 우선한다. encoder 또는 mux open 실패는 다음 후보로 이어진다.
- 캡처 처리 큐가 밀리면 오래된 프레임을 따라잡지 않고 최신 프레임만 처리하며, 드롭 수와 최대 인코더 제출 시간을 진단에 표시한다.
- 디스크 공간 지속 감시 및 쓰기 오류 주입 시험은 남아 있다.

### 12.9 9단계: 스크린샷

작업:

- 현재 캡처 대상에서 단일 프레임을 얻는다.
- 창, 영역 및 모니터에 동일한 대상 모델을 사용한다.
- 필요한 한 프레임만 staging texture로 복사한다.
- WIC로 PNG/JPEG를 저장한다.
- 파일 저장, 클립보드만 복사, 파일 저장 후 클립보드 복사의 세 가지 동작을 제공한다.
- 클립보드 전용 경로는 임시 이미지 파일을 만들지 않고 `CF_DIBV5` 등 Windows 클립보드 메모리 형식으로 직접 전달한다.
- 자동 파일명을 구현한다.

완료 기준:

- 선택한 영역과 저장된 이미지 좌표가 일치한다.
- PNG 해상도와 색상을 검증한다.
- 클립보드 전용 캡처 후 브라우저, 메신저 및 이미지 편집기에 바로 붙여넣을 수 있다.
- 클립보드 전용 캡처는 디스크에 파일이나 임시 파일을 남기지 않는다.
- 스크린샷 단축키를 지원한다.
- 정지 화면 저장이 실시간 녹화에 지속적인 부하를 주지 않는다.

현재 구현 상태 (2026-07-23):

- `Copy to clipboard`, `Save PNG file`, `Save PNG + copy`의 결과 중심 동작을 메인 UI와 실제 캡처 프레임에 연결했다.
- 선택한 창, 모니터 또는 영역을 기존 D3D11 crop 경로로 처리한 뒤 한 프레임만 staging texture로 readback한다.
- WIC PNG 저장과 top-down 32-bit `CF_DIBV5` 클립보드 직접 복사를 구현했다.
- 클립보드 전용 경로는 출력 경로를 만들거나 WIC 파일 인코더를 호출하지 않는다.
- 스크린샷과 녹화는 `%LOCALAPPDATA%\OpenCapture\output_directory.txt`에 저장한 동일 출력 폴더를 사용하며 UI에서 폴더를 변경할 수 있다.
- 영역 오버레이는 Enter 확정 시 자체 창만 닫고 메인 앱 메시지 루프를 종료하지 않는다.
- 앱 재실행 시 저장된 영역 좌표와 해당 모니터 연결을 함께 복원한다.
- GIF 버튼은 선택 대상과 저장된 영역 프리셋을 그대로 사용하며 해상도·FPS·색상 수를 별도로 조절한다.
- 합성 BGRA D3D11 texture를 이용한 자동 스모크에서 64x32 PNG WIC 재열기, `CF_DIBV5` 헤더 및 클립보드 전용 무파일 동작을 검증했다.
- 실제 브라우저, 메신저 및 이미지 편집기 붙여넣기와 1080p 300ms 목표 측정, 전역 단축키는 추가 검증이 필요하다.

### 12.10 10단계: GIF와 포맷 변환

작업:

- 동일 캡처 대상으로 짧은 GIF 녹화를 구현했다.
- 저장된 영역 프리셋에서 즉시 GIF 녹화를 시작할 수 있게 했다.
- 전체 프레임을 메모리에 쌓지 않는 FFmpeg 2-pass palettegen/paletteuse를 적용했다.
- GIF FPS, 출력 높이 및 색상 수 제한을 제공했다.
- 30초와 5억 픽셀 처리 예산 중 먼저 도달하는 시점에 자동 종료한다.
- 변환 성공 전에는 안전 소스 MKV를 지우지 않고 `.part.gif`를 최종 이름으로 확정한다.
- MP4, MKV 및 WebM 변환을 지원한다.
- remux와 재인코딩을 구분한다.
- NVENC 기반 고속 변환 옵션과 진행률 및 취소를 제공한다.

완료 기준:

- 창, 영역 및 모니터 GIF를 만들 수 있다.
- YouTube 등 창 기준 영역 프리셋으로 반복 GIF를 만들 때 캡처 위치가 일관된다.
- GIF 색상과 용량이 적절하다.
- 변환 작업이 UI 스레드와 분리되어 캡처 UI를 멈추지 않는다.

2026-07-24 자동 검증:

- 480p/12fps/128색 종단간 녹화에서 852x480, 13프레임, 1.01초, 101,670바이트 GIF를 생성했다.
- 360p/6fps/64색 설정에서 640x360, 6fps, 95,287바이트 GIF를 생성해 설정 반영을 검증했다.
- 기존 1080p60 소스를 제한 없이 GIF로 만들면 1.03초만으로 3,205,769바이트가 되어 기본 720p/12fps와 경고·자동 종료가 필요한 근거를 확인했다.
- 출력 이름 충돌 시 기존 GIF를 보존하고, 손상 입력은 출력 파일 없이 실패하는 스모크를 통과했다.
- 2-pass 변환 진행률을 원자적으로 UI에 전달하며 `Cancel GIF conversion`으로 중단할 수 있다.
- 480p/12fps 비동기 종단간 스모크에서 852x480, 13프레임, 1.01초, 101,220바이트 GIF를 생성하고 임시 파일이 남지 않음을 확인했다.
- 취소 토큰 스모크에서 최종 GIF를 만들지 않고 종료했으며 앱 종료 시 작업을 취소·join한 뒤 `.part.gif`를 제거한다.
- VP9/Opus 의존성과 MP4/MKV/WebM 범용 재인코딩은 후속 작업이다.

### 12.11 11단계: 캡처 대상 상시 테두리와 툴팁

작업:

- 캡처 엔진과 분리된 `CaptureTargetOverlay` 상태 모델과 Win32 오버레이 창을 구현한다.
- `WS_EX_TOPMOST`, `WS_EX_LAYERED`, `WS_EX_TRANSPARENT`, `WS_EX_TOOLWINDOW`,
  `WS_EX_NOACTIVATE`와 투명 히트 테스트로 클릭과 포커스를 방해하지 않게 한다.
- 영역과 모니터는 실제 캡처 좌표를 사용하고, 창은 DWM 확장 프레임 경계와 위치 변경
  이벤트를 추적하되 낮은 주기의 보정 검사로 누락을 복구한다.
- 대기, 스크린샷, 녹화, 일시정지 및 오류 상태를 테두리 색상으로 연결한다.
- 테두리 창을 캡처 결과에서 제외하고, 지원 여부와 실패를 로그 및 상태 UI에 기록한다.
- 패키징된 빌드에 `graphicsCaptureWithoutBorder` capability를 선언하고, 최초 사용 시
  borderless 캡처 동의를 요청한다.
- 사용자가 동의를 거부하거나 API를 지원하지 않는 경우 Windows 기본 테두리를 유지하고
  제한 사항을 설명하는 폴백을 제공한다.
- 메인 패널의 모든 행동 버튼과 주요 옵션에 결과 중심 툴팁을 추가한다.

완료 기준:

- 대상 선택 직후 앱이 비활성화되거나 최소화되어도 파란 테두리가 항상 위에 유지된다.
- 영역은 지정 사각형, 창은 현재 창 경계, 모니터는 해당 디스플레이 경계와 2픽셀 이내로
  일치한다.
- 창 이동·크기 변경·최대화 시 테두리가 따라가며 최소화·종료 시 잘못된 위치에 남지 않는다.
- 캡처 시작 후 100ms 이내에 노란색으로 전환되고, 스크린샷은 사용자가 인식할 수 있도록
  최소 400ms 표시한 뒤 대기 색상으로 돌아온다.
- 테두리가 마우스, 키보드, 게임 입력 및 Alt+Tab을 방해하지 않고 캡처 결과에도 들어가지 않는다.
- 100%, 125%, 150%, 200% DPI와 음수 좌표를 포함한 다중 모니터에서 좌표를 검증한다.
- borderless 권한을 허용한 영역 캡처에서는 모니터 전체 Windows 테두리가 사라지고 선택
  영역의 OpenCapture 테두리만 보인다.
- 권한 거부 및 미지원 환경에서는 캡처 실패 없이 동작하며 제한 사항이 명확히 표시된다.
- 상시 대기 오버레이의 추가 CPU 사용률은 목표 0.5%p 미만이며 전체 프레임 GPU 작업을 만들지 않는다.
- 버튼 및 주요 설정의 툴팁을 마우스와 키보드로 확인할 수 있고 현재 단축키와 비활성 이유가 정확하다.

현재 구현 상태 (2026-07-27):

- 영역·모니터·창 경계를 따라가는 `CaptureTargetOverlay`를 구현했다. 전체 영역을 덮는
  투명 레이어 창이 일부 Windows 합성 환경에서 보이지 않는 문제를 피하기 위해
  위·아래·왼쪽·오른쪽 네 개의 3픽셀 실선 Win32 창으로 테두리를 구성한다.
- `TOPMOST`, `LAYERED`, `TRANSPARENT`, `TOOLWINDOW`, `NOACTIVATE` 속성으로 항상 위에
  표시하면서 클릭, 포커스 및 Alt+Tab을 방해하지 않게 했다.
- 대기 파란색, 캡처·녹화 노란색, 일시정지 주황색, 오류 빨간색 상태를 연결했으며
  스크린샷 완료 표시는 400ms 유지한다.
- `WDA_EXCLUDEFROMCAPTURE` 적용 성공과 `0x11` 표시 affinity를 실제 실행에서 확인했다.
- 일부 Windows 합성 환경에서 `WDA_EXCLUDEFROMCAPTURE` 창이 화면에서도 보이지 않는
  현상을 확인해, 영역과 창 테두리는 캡처 사각형 바깥에 배치하고 표시 affinity를
  `WDA_NONE`으로 유지한다. 영역은 GPU crop 밖이고 창 캡처는 별도 창을 포함하지 않으므로
  결과 영상에는 들어가지 않는다. 모니터 전체 대상만 캡처 중 조건부 제외를 사용한다.
- Target border 설정에서 표시 여부, 1~12픽셀 굵기와 20~100% 불투명도를 변경하고
  기본값(표시, 3픽셀, 85%)으로 복원할 수 있다. 설정은
  `%LOCALAPPDATA%\OpenCapture\border_settings.txt`에 원자적으로 저장한다.
- 창 경계는 최대 20Hz로 DWM 확장 프레임을 추적하고 최소화·숨김·cloaked 상태에서는
  테두리를 숨긴다. 영역과 모니터는 대상이 바뀔 때만 위치를 다시 계산한다.
- Windows borderless 권한을 비동기로 요청하고 허용되면 `IsBorderRequired(false)`를
  적용하며, 미지원·거부·패키지 capability 부재 시 Windows 전체 테두리가 남는다고 UI에
  명시한다.
- Debug 및 별도 Release 검증 빌드와 CTest를 통과했다.
- 실제 100~200% 혼합 DPI 좌표 오차, 창 이동 추적 지연, 오버레이 CPU 0.5%p 목표와
  패키징된 borderless 권한 허용 시나리오는 실기 검증이 남아 있다.

### 12.12 12단계: 사용자 지정 전역 단축키와 트레이

작업:

- `HotkeyAction`과 `HotkeyBinding` 모델을 만들고 다음 동작을 각각 설정할 수 있게 한다.
  - 설정된 출력 방식으로 현재 대상을 스크린샷 캡처
  - 일회성 영역 선택 후 Quick Capture
  - 비디오 녹화 시작/중지
  - 비디오 일시정지/재개
  - GIF 녹화 시작/중지
  - 마지막 영역 또는 즐겨찾는 영역 프리셋 선택
  - 마이크 음소거/해제
- 설정 화면에서 `단축키 입력`, `지우기`, `기본값 복원`을 제공하고 다음 키 조합을 직접
  눌러 입력하게 한다.
- `RegisterHotKey`와 `WM_HOTKEY`를 사용해 앱이 숨겨져 있어도 동작하게 하고,
  `MOD_NOREPEAT`로 키를 누르고 있을 때 동작이 반복되지 않게 한다.
- 앱 내부 중복, Windows 예약 조합, 다른 프로그램이 이미 등록한 조합을 감지하고 기존
  설정을 덮어쓰지 않은 채 충돌 원인을 표시한다.
- 문자 한 글자만 사용하는 위험한 조합은 허용하지 않고 Ctrl, Alt 또는 Shift 등의
  보조 키를 요구한다. Windows 키 조합은 운영체제 예약 가능성을 안내한다.
- 변경할 때 기존 등록을 해제하고 새 조합 등록에 성공한 경우에만 설정을 원자적으로 저장한다.
- 각 버튼 툴팁과 상태 영역에 현재 단축키를 표시한다.
- 영역 선택 및 단축키 입력 모드에서는 일반 단축키 실행을 막되 녹화 중지용 비상 단축키는
  상태 머신을 통해 안전하게 처리한다.
- 시스템 트레이에 캡처 상태, 시작/중지 및 앱 열기 메뉴를 제공한다.

완료 기준:

- 앱 창이 숨겨져 있거나 포커스가 게임·브라우저에 있어도 설정한 스크린샷, 비디오 및 GIF
  단축키가 동작한다.
- 동일 키를 길게 눌러도 한 번만 실행되며 빠른 연속 입력이 중복 세션을 만들지 않는다.
- 충돌한 단축키는 명확한 오류를 표시하고 이전에 정상 등록된 단축키는 계속 동작한다.
- 앱 재시작 후 사용자 설정이 복원되고, 설정 파일이 손상되면 안전한 기본값으로 복구한다.
- 녹화 중 시작, 변환 중 GIF 시작 등 허용되지 않는 요청은 상태 머신이 거부하고 이유를 표시한다.
- 일반적인 게임 단축키와 충돌하면 설정 화면에서 즉시 변경하고 재등록 결과를 확인할 수 있다.
- 녹화 중 단축키와 트레이 UI가 불필요한 GPU 부하를 만들지 않는다.

현재 구현 상태 (2026-08-21):

- `Ctrl+Shift+F9` 클립보드 스크린샷, `Ctrl+Shift+F10` 비디오 시작/중지,
  `Ctrl+Shift+F11` GIF 시작/중지, `Ctrl+Shift+F8` Quick Capture를 기본값으로 등록한다.
- Settings에서 `Set shortcut`을 누른 뒤 원하는 키 조합을 직접 입력해 단축키를 지정한다.
  글자, 숫자, 기능 키, 방향키 등 보조 키와 함께 쓰는 조합을 허용하며 `Clear`와
  동작별 `Restore Default`를 제공한다.
- 단축키 입력 중에는 전역 단축키를 잠시 해제하고 Esc 또는 포커스 상실 시 입력을 취소한다.
- `RegisterHotKey`와 `MOD_NOREPEAT`를 사용하며 앱 내부 중복 또는 다른 프로그램과의
  충돌 시 기존 등록을 복구하고 변경을 저장하지 않는다.
- 단축키는 `%LOCALAPPDATA%\OpenCapture\hotkeys.txt`에, 비디오/GIF 설정은
  `recording_settings.txt`에 임시 파일과 원자적 교체 방식으로 저장하고 재시작 시 복원한다.
- Settings의 `Restore Default`는 단축키, 비디오, GIF, 스크린샷 출력, UI 배율, 테두리,
  영역 선택 화면, 트레이 상주 설정을 원래 기본값으로 되돌린다.
- 버튼과 FPS, 품질, 오디오, 출력 폴더, GIF 용량 설정에 결과 중심 툴팁을 추가했다.
- 영역 프리셋의 `Apply`는 선택 오버레이와 Enter 확인을 다시 열지 않고 저장 좌표 또는
  현재 창 기준으로 변환한 좌표를 즉시 캡처 대상에 적용한다.
- 스크린샷 단축키 출력 방식과 영구 대상을 바꾸지 않는 Quick Capture를 구현했으며
  사용자 실기 검증을 통과했다.
- 시스템 트레이에서 앱 열기, Quick Capture, 현재 녹화 중지와 완전 종료를 실행할 수 있다.
  신규 사용자의 기본 닫기 동작은 완전 종료이며 Settings에서 명시적으로 켠 경우에만
  트레이에 상주한다. 기존 저장 설정은 유지되고 변경은 원자적으로 저장된다.
- Explorer 재시작 후 트레이 아이콘을 복구하고, 트레이 생성·복구 실패 시 창 닫기가 정상
  종료로 폴백한다. 숨겨진 유휴 상태와 최소화된 창에서는 메시지를 기다리거나 UI를
  10Hz로 제한해, DXGI가 vsync를 건너뛸 때 렌더 루프가 바쁘게 돌지 않게 한다.
- 즐겨찾기 프리셋별 단축키와 마이크 음소거 단축키는 후속 작업이다.

### 12.13 13단계: 스크린샷 작업 흐름과 UI 재구성

이 단계는 기능을 한꺼번에 연결하지 않고 상태 모델, 선택 수명주기, Quick Capture,
UI 재구성 순으로 진행한다. 각 하위 단계가 독립적으로 빌드·테스트를 통과한 뒤 다음으로
넘어간다.

#### 12.13.1 단축키 출력 설정과 요청 모델

작업:

- `ScreenshotRequest`에 출력 방식, 대상 및 요청 출처를 함께 담아 스크린샷 진입점을 통합한다.
- 현재 `ScreenshotClipboard` 단축키를 기존 등록 인덱스와 키 조합을 유지한 채
  일반 `Screenshot` 동작으로 마이그레이션한다.
- 클립보드, PNG 저장, 저장+복사 설정을 원자적으로 저장하고 손상 값의 기본값 복구를 구현한다.
- 메인 UI의 세 개 즉시 실행 동작은 설정값과 무관하게 기존의 명시적 결과를 유지한다.

완료 기준:

- 같은 전역 단축키가 선택한 출력 방식에 따라 세 결과를 정확히 만든다.
- 클립보드 모드에서는 PNG와 임시 파일이 생기지 않고, 파일 모드에서는 클립보드를 바꾸지 않는다.
- 저장+복사는 하나의 캡처 프레임을 재사용하며 PNG와 클립보드 이미지 내용이 일치한다.
- 앱 재시작, 설정 파일 없음, 알 수 없는 값 및 잘린 설정 파일을 검증한다.
- 기존 `hotkeys.txt`를 가진 사용자의 F9 단축키가 초기화되거나 다른 기능으로 이동하지 않는다.

#### 12.13.2 Region 선택 창 숨김과 복원

작업:

- 메인 창 상태를 저장·숨김·복원하는 범위 기반 가드를 구현한다.
- 일반 Region 선택과 Quick Capture가 동일한 숨김/복원 수명주기를 사용하게 한다.
- 선택 직전 상시 대상 테두리를 숨기고 선택 종료 후 현재 영구 대상에 맞게 다시 계산한다.
- 확정, Escape, 오버레이 생성 실패, 모니터 변경 및 앱 종료 경로를 하나의 정리 함수로 모은다.

완료 기준:

- 메인 창이 선택 가능한 화면 영역을 가리지 않고 선택 결과에도 포함되지 않는다.
- 일반 Region 확정 후 메인 창의 위치, 크기, 최소화/최대화 상태가 선택 전과 같다.
- Escape와 실패 후 기존 대상, 상시 테두리, 단축키 등록과 앱 실행 상태가 정상이다.
- 백그라운드에서 시작한 선택은 완료 후 메인 창을 임의로 표시하거나 포커스를 빼앗지 않는다.
- 선택을 20회 반복해도 숨은 창, 남은 오버레이, 포커스 고착 또는 핸들 증가가 없다.

#### 12.13.3 Quick Capture

작업:

- `HotkeyAction::QuickCapture`와 기본값 `Ctrl+Shift+F8`을 추가하고 충돌 검사·저장을 연결한다.
- `CaptureTargetPicker`에 저장 부작용이 없는 `PickTemporaryRegion`을 추가한다.
- Quick Capture 상태를 `Idle → Selecting → Capturing → Completing/Failed → Idle`로 관리한다.
- 선택 완료 후 임시 대상과 단축키 출력 설정으로 기존 GPU 스크린샷 경로를 실행한다.
- 진행 중 작업과의 충돌 정책을 중앙 상태 검사로 구현하고 거부 이유를 공통 상태에 표시한다.

상태 충돌 정책:

| 현재 상태 | Quick Capture 처리 |
|---|---|
| 대기 중 | 즉시 영역 선택 시작 |
| 다른 영역 선택 중 | 중복 요청 무시 |
| 스크린샷 프레임 대기 중 | 완료 전까지 거부 |
| 비디오/GIF 녹화 또는 일시정지 | 기존 세션 보호를 위해 거부 |
| GIF 변환/remux/복구 작업 중 | 미디어 작업 완료 전까지 거부 |
| 앱 종료 중 | 요청 폐기하고 오버레이 정리 |

완료 기준:

- 앱이 앞에 있거나 뒤에 있거나 최소화되어 있어도 단축키로 선택 오버레이만 나타난다.
- 확정 후 설정한 출력 방식으로 정확히 한 장을 캡처하고, Escape는 아무 부작용 없이 끝난다.
- Quick Capture 전후 영구 대상, Region 프리셋 파일, 대상 테두리와 선택 라벨이 동일하다.
- 음수 좌표를 포함한 다중 모니터와 100~200% 혼합 DPI에서 좌표 오차가 2픽셀 이하다.
- 1080p 영역은 Enter 확정 후 클립보드 완료 목표 300ms 이내이며 대기 중 추가 GPU 부하는 없다.
- 빠른 단축키 반복, Enter 연타 및 Escape와 단축키 동시 입력이 중복 캡처를 만들지 않는다.

#### 12.13.4 UI 정보 구조 재구성

작업:

- 긴 단일 패널을 Capture, Video, GIF, Settings 탭과 공통 헤더·상태바로 분리한다.
- `MainPanelState`를 만들고 정적 지역 변수에 흩어진 편집 상태를 명시적으로 소유한다.
- 캡처 대상과 프리셋, 녹화, GIF, 단축키, 디스플레이/오버레이, 고급 진단 코드를
  독립 그리기 함수로 나눈다.
- 좁은 창 및 고 DPI에서는 자동 줄바꿈·세로 배치하고, 사용 빈도가 낮은 설정은 접는다.
- 기존 기능의 명령 구조는 유지해 UI 재배치가 캡처·인코더 구현을 직접 변경하지 않게 한다.

완료 기준:

- 앱 실행 후 두 번 이내의 클릭으로 고정 대상 스크린샷, 비디오, GIF를 시작할 수 있다.
- Quick Capture는 현재 탭과 관계없이 한 번의 버튼 또는 단축키로 시작할 수 있다.
- 기본 Capture 화면에는 녹화 인코더, GIF 색상, 복구 도구 같은 비관련 설정이 보이지 않는다.
- 1080p 100%, 1440p 150%, 4K 200%에서 잘린 텍스트·겹친 버튼·가로 스크롤이 없다.
- 모든 기존 버튼과 설정의 동작을 새 위치에서 찾을 수 있고 기능 누락 회귀 검사를 통과한다.
- 키보드 Tab 이동, Enter 실행, Escape 취소 및 툴팁 접근을 확인한다.

권장 구현 순서:

1. 스크린샷 출력 설정 모델과 단위 테스트
2. 요청 객체 기반 스크린샷 경로 통합
3. 메인 창 숨김/복원 가드와 일반 Region 선택 적용
4. 부작용 없는 임시 Region 선택 API
5. Quick Capture 상태 머신·단축키·충돌 처리
6. MainPanel 상태 분리와 탭 기반 UI 재배치
7. Debug/Release CTest, 스크린샷 스모크 및 실제 다중 모니터 수동 검증
8. 문서·릴리스 노트·사용 화면 갱신

현재 구현 상태 (2026-07-27):

- 기존 F9 단축키 인덱스와 사용자 키 조합을 유지하면서 출력 결과를 클립보드, PNG 또는
  저장+복사로 선택하고 원자적으로 저장하는 설정을 추가했다.
- 스크린샷 요청에 출력 방식, 캡처 대상과 요청 출처를 함께 담아 UI, 고정 대상 단축키와
  Quick Capture가 같은 GPU/WIC/클립보드 경로를 사용한다.
- 일반 Region 선택 직전에 메인 창과 상시 테두리를 숨기고, 확정·취소 후 이전 창 배치,
  표시 여부, 포커스와 Z 순서를 복원한다.
- `PickTemporaryRegion`은 영구 대상과 프리셋 파일을 쓰지 않으며, Quick Capture는
  기본 `Ctrl+Shift+F8` 단축키와 선택한 스크린샷 결과 설정을 사용한다.
- Quick Capture는 녹화, GIF, 변환 또는 다른 스크린샷과 충돌하면 기존 작업을 중단하지
  않고 상태 메시지로 거부한다.
- 메인 패널을 Capture, Video, GIF, Settings 탭과 공통 대상·출력·Quick Capture 헤더로
  분리하고 편집 상태를 명시적인 `MainPanelState`로 모았다.
- 좁은 창에서는 스크린샷 버튼을 세로로 배치하고, 녹화 중지·일시정지는 어느 탭에서도
  공통 헤더에서 접근할 수 있다.
- Debug 빌드와 CTest를 통과했고 실제 4K 탭 배치, Region 선택 시 창 숨김 및
  Quick Capture 작업 흐름은 사용자 실기 검증을 통과했다.

### 12.14 14단계: Desktop Duplication 폴백

작업:

- Windows Graphics Capture 지원 여부와 실패를 감지한다.
- DXGI Output과 Desktop Duplication을 연결한다.
- 모니터 회전과 하드웨어 포인터를 처리한다.
- 기존 GPU 처리 및 인코더 파이프라인을 재사용한다.

완료 기준:

- WGC가 실패하는 환경에서 모니터 전체를 녹화할 수 있다.
- 자동, WGC, DXGI 캡처 방식을 설정에서 선택할 수 있다.
- 캡처 구현이 바뀌어도 인코더 코드는 변경되지 않는다.

## 13. 성능 및 안정성 검증

### 13.1 시험 환경

- NVIDIA 단독 GPU
- Intel 내장 GPU
- AMD GPU
- 내장 GPU와 외장 GPU를 함께 사용하는 노트북
- 1080p 60Hz
- 1440p 고주사율
- 4K 60Hz
- SDR 및 HDR
- 단일 및 다중 모니터
- 일반 창, 경계 없는 전체 화면 및 모니터 전체
- 브라우저의 YouTube 영상 영역 프리셋 반복 캡처
- 개발 중인 게임의 동일 장면 또는 재현 가능한 벤치마크 구간

### 13.2 측정 항목

- 녹화 전후 대상 애플리케이션 평균 FPS
- 1% low FPS
- 캡처 및 인코딩 프레임 드롭률
- 프레임 큐 길이와 지연
- GPU Video Encode 사용률
- GPU 3D 및 Copy 사용률
- CPU 사용률
- 메모리와 GPU 메모리 증가
- 오디오/영상 동기화 오차
- 장시간 녹화 후 파일 정상성
- 기준 구간 대비 녹화 중 평균 FPS, 1% low 및 프레임 타임 변화율
- 저부하 모드에서 OpenCapture의 CPU, GPU 3D, Copy 및 Video Encode 사용량
- 클립보드 전용 캡처 지연 시간과 디스크 쓰기 발생 여부

### 13.3 초기 합격 기준

```text
1080p 60fps
- 프레임 드롭률 0.1% 미만
- NVIDIA 환경에서 NVENC 사용
- 게임 FPS 영향 목표 5% 이내
- 저부하 모드에서 1% low 저하 목표 8% 이내
- 녹화로 인한 추가 CPU 사용률 목표 5%p 이내

1440p 60fps
- 프레임 드롭률 0.5% 미만
- 캡처 및 인코딩 큐가 지속해서 증가하지 않음

4K 60fps
- 지원되는 NVIDIA GPU에서 NVENC 사용
- 전체 프레임 GPU-to-CPU 실시간 복사 없음

장시간 녹화
- 2시간 동안 지속적인 메모리 증가 없음
- 오디오/영상 오차 50ms 이내
- 녹화 중지 후 결과 파일 정상 재생

영역 프리셋 및 클립보드
- 창 기준 프리셋을 창 이동 후 적용해도 목표 영역 오차 2픽셀 이내
- 동일 프리셋의 스크린샷과 GIF가 동일한 영역을 사용
- 클립보드 전용 캡처에서 이미지 파일 및 임시 파일 생성 없음
- 1080p 영역의 클립보드 캡처 완료 목표 300ms 이내

영역 선택 오버레이
- 선택 내부는 투명하게 유지하고 바깥 어둡기 기본값 30%
- 0%, 30%, 70% 어둡기에서 선택선, 원본 내용 및 크기 안내 식별 가능
- 100~200% 혼합 DPI와 음수 좌표 모니터에서 최종 캡처 좌표 오차 2픽셀 이내
- 모니터 경계를 넘긴 선택이 조용히 잘린 결과를 만들지 않음
- Enter, Escape 및 반복 선택 후 메인 창, 상시 테두리와 단축키 상태 정상 복원

대상 테두리와 단축키
- 대기 중 상시 테두리의 추가 CPU 사용률 0.5%p 미만
- 캡처 상태 변경 후 테두리 색상 반영 100ms 이내
- 테두리 좌표와 실제 캡처 좌표 오차 2픽셀 이내
- 테두리 픽셀이 PNG, 비디오 및 GIF 결과에 포함되지 않음
- 전역 단축키 자동 반복, 충돌, 재시작 복원 및 상태 충돌 시험 통과

스크린샷 단축키와 Quick Capture
- 클립보드, PNG, PNG+클립보드 세 출력 방식의 파일·클립보드 부작용을 각각 검증
- 일반 Region 선택 중 메인 창과 기존 대상 테두리가 보이지 않고 종료 후 원상 복원
- Quick Capture 전후 영구 대상, 프리셋 파일 및 상시 대상 테두리 불변
- 앱 표시, 최소화, 백그라운드 상태에서 Quick Capture의 포커스와 창 표시 상태 보존
- 선택 취소, 모니터 분리, 오버레이 생성 실패 및 앱 종료 중 임시 창·캡처 세션 잔존 없음
- 20회 반복 선택과 빠른 단축키 입력에서 중복 파일, 중복 클립보드 쓰기 및 핸들 누수 없음
- 1080p 임시 영역의 Enter 확정 후 클립보드 완료 목표 300ms 이내

UI 재구성
- Capture, Video, GIF, Settings 각 탭에서 기존 기능 명령 매핑 회귀 검사
- 100%, 150%, 200% DPI에서 최소 창 폭과 기본 창 폭의 잘림·겹침 검사
- 마우스 없이 탭 이동, 주요 버튼 실행, 설정 변경 및 Escape 취소 가능
- 가장 자주 쓰는 고정 대상 캡처와 Quick Capture는 현재 탭과 무관하게 즉시 접근 가능
```

FPS 영향 수치는 게임, GPU 사용률, 해상도 및 인코더 세대에 따라 달라지므로 실제 시험 결과를 함께 기록한다.

## 14. 로깅과 진단

- 세션별 로그 파일을 생성한다.
- GPU, 드라이버, 캡처 방식 및 인코더 정보를 기록한다.
- 캡처 FPS, 인코딩 FPS, 프레임 드롭 및 큐 지연을 기록한다.
- FFmpeg 오류 코드를 사람이 이해할 수 있는 메시지로 변환한다.
- 민감한 창 제목과 파일 경로는 진단 정보 내보내기 시 선택적으로 제거한다.

## 15. 라이선스 검토

- OpenCapture는 오픈소스 라이선스로 공개한다.
- Dear ImGui, FFmpeg 및 기타 의존성의 라이선스와 고지 파일을 배포물에 포함한다.
- FFmpeg 배포 빌드의 LGPL/GPL 구성 옵션을 명확하게 기록한다.
- NVIDIA Video Codec SDK 헤더 및 런타임 사용 조건을 검토한다.
- 특허가 적용될 수 있는 H.264, HEVC 및 AAC 배포 조건은 릴리스 전에 별도로 검토한다.

## 16. 최종 결정 요약

OpenCapture는 게임 전용 내부 캡처기가 아니라 모든 Windows 창, 사용자 지정 영역 및 모니터 전체를 대상으로 하는 고성능 화면 캡처 애플리케이션으로 개발한다.

핵심 구현은 C++20, Windows Graphics Capture, Direct3D 11 및 FFmpeg 하드웨어 인코딩을 사용한다. NVIDIA 환경에서는 NVENC를 기본으로 선택하며, 화면 프레임을 CPU raw video로 변환해 FFmpeg 프로세스에 전달하지 않는다. 녹화와 스크린샷은 동일한 캡처 대상과 GPU 처리 경로를 공유한다.

반복 작업을 위해 화면 절대 좌표 및 창 기준 상대 좌표 영역 프리셋을 제공하고, 저장된 영역에서 스크린샷과 GIF를 빠르게 실행한다. 스크린샷은 파일 저장 없이 클립보드로만 전달할 수 있다. 게임 플레이 및 성능 테스트에는 저부하 녹화 모드를 제공하며 평균 FPS, 1% low 및 프레임 타임 변화로 녹화 오버헤드를 검증한다.

전역 스크린샷 단축키는 클립보드, PNG 저장 또는 둘 다를 사용자 설정으로 선택한다.
Quick Capture는 앱을 전면에 표시하지 않고 임시 영역을 선택해 한 장만 캡처하며 영구 대상과
Region 프리셋을 변경하지 않는다. 메인 UI는 Dear ImGui를 유지하되 Capture, Video, GIF,
Settings의 작업 중심 구조로 재구성하고, 접근성 요구가 현재 라이브러리 한계를 넘을 때만
네이티브 설정 창을 별도로 검토한다.
